package com.bank.filerenamer.integration;

import org.junit.jupiter.api.Test;

/** Verifica que el contexto de Spring arranca sobre infraestructura real (PostgreSQL + LocalStack). */
class ApplicationContextIT extends AbstractIntegrationTest {

    @Test
    void contextLoads() {
    }
}
